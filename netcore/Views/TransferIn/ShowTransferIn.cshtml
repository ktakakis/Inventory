@model netcore.Models.Invent.TransferIn

@{
    Layout = "_AdminlteNetcore";
}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            Παραλαβή εμπορευμάτων
            <small>@Model.transferInNumber</small>
        </h1>

    </section>
    <div class="pad margin no-print">
        <div class="callout callout-info" style="margin-bottom: 0!important;">
            <h4><i class="fa fa-info"></i> Σημείωση:</h4>
            Αυτή η σελίδα έχει βελτιωθεί για εκτύπωση. Κάντε κλικ στο κουμπί εκτύπωσης στο κάτω μέρος για εκτύπωση.
        </div>
    </div>
    <!-- Main content -->
    <section class="invoice">
        <!-- title row -->
        <div class="row">
            <div class="col-xs-12">
                <h2 class="page-header">
                    <i class="fa fa-globe"></i> Παραλαβή εμπορευμάτων:
                    <small class="pull-right">Ημερομηνία: @Model.transferInDate.ToString("dd-MMM-yyyy")</small>
                </h2>
            </div>
            <!-- /.col -->
        </div>
        <!-- info row -->
        <div class="row invoice-info">
            <div class="col-sm-4 invoice-col">
                Από την αποθήκη
                <address>
                    <strong>@Model.warehouseFrom.warehouseName</strong><br>
                    @Model.warehouseFrom.street1<br>
                    @Model.warehouseFrom.street2<br>
                    @Model.warehouseFrom.city<br>
                    attn: @Model.transferOrder.picName
                </address>
            </div>
            <!-- /.col -->
            <div class="col-sm-4 invoice-col">
                Στην αποθήκη
                <address>
                    <strong>@Model.warehouseTo.warehouseName</strong><br>
                    @Model.warehouseTo.street1<br>
                    @Model.warehouseTo.street2<br>
                    @Model.warehouseTo.city<br>
                    attn: @Model.transferOrder.picName
                </address>
            </div>
            <!-- /.col -->
            <div class="col-sm-4 invoice-col">
                <b>Αριθμός παραλαβής εμπορευμάτων:</b> @Model.transferInNumber<br>
                 <b>Ημερομηνία παραλαβής εμπορευμάτων:</b> @Model.transferInDate.ToString("dd-MMM-yyyy")<br>
                <b>Αριθμός TO:</b> @Model.transferOrder.transferOrderNumber<br>
                <b>Ημερομηνία TO:</b> @Model.transferOrder.transferOrderDate.ToString("dd-MMM-yyyy")<br>
                <b>PIC:</b> @Model.transferOrder.picName<br>

            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
        <!-- Table row -->
        <div class="row">
            <div class="col-xs-12 table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Ποσ</th>
                            <th>Προϊόν</th>
                            <th>Περιγραφή</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.transferInLine)
                        {
                            <tr>
                                <td>@item.qty</td>
                                <td>@item.product.productCode</td>
                                <td>@item.product.productName</td>
                            </tr>
                        }

                    </tbody>
                </table>
            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
        <div class="row">
            <!-- accepted payments column -->
            <div class="col-xs-6">
                <p class="lead">Περιγραφές:</p>
                <p class="text-muted well well-sm no-shadow" style="margin-top: 10px;">
                    @Model.description
                </p>
            </div>
            <!-- /.col -->
            <div class="col-xs-6">

            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
        <!-- this row will not appear when printing -->
        <div class="row no-print">
            <div class="col-xs-12">

                <a href="@Url.Action("PrintTransferIn", new { id = Model.transferInId })" target="_blank" class="btn btn-primary"><i class="fa fa-print"></i> Εκτύπωση</a>

            </div>
        </div>
    </section>
    <!-- /.content -->
    <div class="clearfix"></div>
</div>
<!-- /.content-wrapper -->
