@model netcore.Models.ApplicationUser
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@inject netcore.Data.ApplicationDbContext Context
@{

    ApplicationUser appUser = await UserManager.GetUserAsync(User);

}
<aside class="main-sidebar">
    <!-- sidebar: style can be found in sidebar.less -->
    <section class="sidebar">
        <!-- Sidebar user panel -->
        @if (Model != null)
        {
            <div class="user-panel">
                <div class="pull-left image">
                    <img src="@Model.profilePictureUrl" class="img-circle profilePicture" alt="User Image">
                </div>
                <div class="pull-left info">
                    <p>@Model.Email</p>
                    <a href="#"><i class="fa fa-circle text-success"></i> Σε σύνδεση</a>
                </div>
            </div>

        }

        @if (appUser != null)
        {
    <ul class="sidebar-menu" data-widget="tree">


        <li class="header">ΔΙΑΧΕΙΡΙΣΗ ΑΠΟΘΕΜΑΤΩΝ</li>
        @if (appUser.HomeRole)
        {
            <li class="@Html.IsSelected(controller: "Home")">
                <a href="@Url.Action("Index", "Home")">
                    <i class="fa fa-bar-chart"></i> <span>Αρχική</span>
                </a>
            </li>
        }

        @if (appUser.CitiesRole)
        {
            <li class="@Html.IsSelected(controller: "Cities")">
                <a href="@Url.Action("Index", "Cities")">
                    <i class="fa fa-universal-access"></i> <span>Πόλεις</span>
                </a>
            </li>

        }

        @if (appUser.CatalogRole)
        {
            <li class="@Html.IsSelected(controller: "Catalog")">
                <a href="@Url.Action("Index", "Catalog")">
                    <i class="fa fa-address-book"></i> <span>Κατάλογοι</span>
                </a>
            </li>
        }
        @if (appUser.EmployeeRole)
        {
            <li class="@Html.IsSelected(controller: "Employee")">
                <a href="@Url.Action("Index", "Employee")">
                    <i class="fa fa-user"></i> <span>Συνεργάτες</span>
                </a>
            </li>
        }

        @if (appUser.BranchRole)
        {
            <li class="@Html.IsSelected(controller: "Branch")">
                <a href="@Url.Action("Index", "Branch")">
                    <i class="fa fa-building-o"></i> <span>Υποκαταστήματα</span>
                </a>
            </li>
        }


        @if (appUser.WarehouseRole)
        {
            <li class="@Html.IsSelected(controller: "Warehouse")">
                <a href="@Url.Action("Index", "Warehouse")">
                    <i class="fa fa-cubes"></i> <span>Αποθήκες</span>
                </a>
            </li>
        }

        @if (appUser.ProductRole)
        {
            <li class="@Html.IsSelected(controller: "Product")">
                <a href="@Url.Action("Index", "Product")">
                    <i class="fa fa-tags"></i> <span>Προϊόντα</span>
                </a>
            </li>
        }

        @if (appUser.StockRole)
        {
            <li class="@Html.IsSelected(controller: "Stock")">
                <a href="@Url.Action("Index", "Stock")">
                    <i class="fa fa-cube"></i> <span>Απόθεματα</span>
                </a>
            </li>
        }

        @if (appUser.VendorRole)
        {
            <li class="@Html.IsSelected(controller: "Vendor")">
                <a href="@Url.Action("Index", "Vendor")">
                    <i class="fa fa-industry"></i> <span>Προμηθευτές</span>
                </a>
            </li>
        }

        @if (appUser.PurchaseOrderRole)
        {
            <li class="@Html.IsSelected(controller: "PurchaseOrder")">
                <a href="@Url.Action("Index", "PurchaseOrder")">
                    <i class="fa fa-file-text"></i> <span> Αγορές</span>
                </a>
            </li>
        }

        @if (appUser.ReceivingRole)
        {
            <li class="@Html.IsSelected(controller: "Receiving")">
                <a href="@Url.Action("Index", "Receiving")">
                    <i class="fa fa-truck"></i> <span>Παραλαβές</span>
                </a>
            </li>
        }

        @if (appUser.CustomerRole)
        {
            <li class="@Html.IsSelected(controller: "Customer")">
                <a href="@Url.Action("Index", "Customer")">
                    <i class="fa fa-bank"></i> <span>Πελάτες</span>
                </a>
            </li>
        }

        @if (appUser.SalesOrderRole)
        {
            <li class="@Html.IsSelected(controller: "SalesOrder")">
                <a href="@Url.Action("Index", "SalesOrder")">
                    <i class="fa fa-cart-plus"></i> <span> Πωλήσεις</span>
                </a>
            </li>
        }

        @if (appUser.ShipmentRole)
        {
            <li class="@Html.IsSelected(controller: "Shipment")">
                <a href="@Url.Action("Index", "Shipment")">
                    <i class="fa fa-plane"></i><span> Αποστολές</span>
                </a>
            </li>
        }

        @if (appUser.InvoiceRole)
        {
            <li class="@Html.IsSelected(controller: "Invoice")">
                <a href="@Url.Action("Index", "Invoice")">
                    <i class="fa fa-certificate"></i><span> Τιμολόγια</span>
                </a>
            </li>
        }

        @if (appUser.PaymentTypeRole)
        {
            <li class="@Html.IsSelected(controller: "PaymentType")">
                <a href="@Url.Action("Index", "PaymentType")">
                    <i class="fa fa-get-pocket"></i><span> Τρόποι Είσπραξης</span>
                </a>
            </li>
        }

        @if (appUser.PaymentReceiveRole)
        {
            <li class="@Html.IsSelected(controller: "PaymentReceive")">
                <a href="@Url.Action("Index", "PaymentReceive")">
                    <i class="fa fa-money"></i><span> Εισπράξεις</span>
                </a>
            </li>
        }
        @if (appUser.PaymentReceiveRole)
        {
            <li class="@Html.IsSelected(controller: "VendorPayment")">
                <a href="@Url.Action("Index", "VendorPayment")">
                    <i class="fa fa-money"></i><span> Πληρωμές Προμηθευτών</span>
                </a>
            </li>
        }

        @if (appUser.TransferOrderRole)
        {
            <li class="@Html.IsSelected(controller: "TransferOrder")">
                <a href="@Url.Action("Index", "TransferOrder")">
                    <i class="fa fa-recycle"></i> <span> Μεταφορές</span>
                </a>
            </li>
        }

        @if (appUser.TransferOutRole)
        {
            <li class="@Html.IsSelected(controller: "TransferOut")">
                <a href="@Url.Action("Index", "TransferOut")">
                    <i class="fa fa-upload"></i> <span> Εξαγωγές</span>
                </a>
            </li>
        }

        @if (appUser.TransferInRole)
        {
            <li class="@Html.IsSelected(controller: "TransferIn")">
                <a href="@Url.Action("Index", "TransferIn")">
                    <i class="fa fa-download"></i> <span> Εισαγωγές</span>
                </a>
            </li>
        }

        @if (appUser.CashRepositoryRole)
        {
            <li class="@Html.IsSelected(controller: "CashRepository")">
                <a href="@Url.Action("Index", "CashRepository")">
                    <i class="fa fa-google-wallet"></i> <span> Ταμεία</span>
                </a>
            </li>
        }

        @if (appUser.CashRepositoryRole)
        {
            <li class="@Html.IsSelected(controller: "MoneyTransferOrder")">
                <a href="@Url.Action("Index", "MoneyTransferOrder")">
                    <i class="fa fa-exchange"></i> <span> Μεταφορές Χρημάτων</span>
                </a>
            </li>
        }
        @if (appUser.CashflowInRole)
        {
            <li class="@Html.IsSelected(controller: "CashflowIn")">
                <a href="@Url.Action("Index", "CashflowIn")">
                    <i class="fa fa-arrow-circle-down"></i> <span> Ταμειακές Εισροές</span>
                </a>
            </li>
        }

        @if (appUser.CashflowOutRole)
        {
            <li class="@Html.IsSelected(controller: "CashflowOut")">
                <a href="@Url.Action("Index", "CashflowOut")">
                    <i class="fa fa-arrow-circle-up"></i> <span> Ταμειακές Εκροές</span>
                </a>
            </li>
        }

        @if (appUser.CategoryRole)
        {
            <li class="@Html.IsSelected(controller: "Category")">
                <a href="@Url.Action("Index", "Category")">
                    <i class="fa fa-list"></i> <span> Κατηγορίες</span>
                </a>
            </li>

        }

        @if (appUser.ApplicationUserRole)
        {
            <li class="header">ΡΥΘΜΙΣΕΙΣ</li>
            <li class="@Html.IsSelected(controller: "ApplicationUser")">
                <a href="@Url.Action("Index", "ApplicationUser")">
                    <i class="fa fa-users"></i> <span>Ρόλοι χρηστών</span>
                </a>
            </li>
            <li class="@Html.IsSelected(controller: "NumberSequence")">
                <a href="@Url.Action("Index", "NumberSequence")">
                    <i class="fa fa-barcode"></i> <span> Σειρές Αριθμών</span>
                </a>
            </li>
        }

    </ul>

        }

    </section>
    <!-- /.sidebar -->
</aside>